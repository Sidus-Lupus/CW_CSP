#CW Integers and Floats

"""price = float(input("Enter original price: $"))

tax_rate = float(input("Enter tax percent: %"))/100

total_price = price*(1+ tax_rate)

print(round(total_price, 2))"""

apples = int(input("How many apples are there: "))

friends = int(input("How many friends are there: "))

print("Each friend gets", apples//friends, "apples and I have", apples%friends, "to myself")
